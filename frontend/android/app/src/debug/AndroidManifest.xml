<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Development only: permits the emulator to call the local HTTP Flask API. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
